version: '3.8'

services:
  rag-system:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: local-rag-system
    ports:
      - "9292:9292"
    volumes:
      # Persist vector database
      - /mnt/user/appdata/llm/chroma:/app/data/chroma
      # Persist uploaded documents
      - /mnt/user/appdata/llm/uploads:/app/data/uploads
      # Persist extracted images
      - /mnt/user/appdata/llm/images:/app/data/images
    environment:
      # Override any .env settings if needed
      - APP_HOST=0.0.0.0
      - APP_PORT=9292
      - LOG_LEVEL=INFO
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    devices:
      - /dev/dri:/dev/dri
      - /dev/kfd:/dev/kfd # Only if required by your application
    # Resource limits (adjust based on your system)
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 8G
        reservations:
          cpus: '2'
          memory: 4G
